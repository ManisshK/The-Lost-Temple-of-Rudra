# THE LOST TEMPLE OF RUDRA — Python Dependencies
# Python 3.11+ required.

# --- Testing ---
pytest>=9.0

# --- GUI (tkinter is stdlib — no install needed) ---
# tkinter is bundled with CPython on Windows, macOS, and most Linux distros.
# If your Python build lacks tkinter, install it via your system package manager.

# --- Audio (optional — silently disabled if absent) ---
# pygame>=2.5          # Audio engine — uncomment to enable sound

# --- AI / LLM (optional — OllamaProvider uses stdlib urllib) ---
# requests>=2.31       # Alternative HTTP client for Ollama (optional)

# --- Packaging (optional — only needed to build distributable) ---
# pyinstaller>=6.0     # Windows/macOS/Linux executable builder

# --- Data Validation (optional) ---
# pydantic>=2.0        # World Model schema validation

# --- Terminal Formatting (optional — CLI only) ---
# rich>=13.0           # Coloured terminal output
