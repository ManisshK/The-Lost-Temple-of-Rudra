"""
ui/__init__.py — The Lost Temple of Rudra

UI package.
Contains the main window, menu, inventory panel, journal, and dialogue system.
The UI is a presentation layer only — it reads from the World Model and displays.
It never writes to the World Model.
"""
