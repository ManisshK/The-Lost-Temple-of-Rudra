# THE LOST TEMPLE OF RUDRA — Python Dependencies

# --- Testing ---
pytest>=9.0

# --- AI / LLM (optional — used by OllamaProvider when Ollama is enabled) ---
# The OllamaProvider uses Python's built-in urllib — no external HTTP library
# required for basic operation. If you prefer requests, uncomment below.
# requests          # HTTP client for Ollama API (optional alternative)

# --- UI Framework (confirm before Phase 7) ---
# pygame
# customtkinter

# --- Data Validation ---
# pydantic          # World Model schema validation

# --- Terminal Formatting (optional text UI) ---
# rich
